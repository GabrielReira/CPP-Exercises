#include <iostream>
using namespace std;

int main()
{
    int k1, k2, k3, k4, k5;
    int leisure;

    cin >> k1 >> k2 >> k3 >> k4 >> k5;

    leisure = (k1 * 2) + (k2 * 2) + (k3 * 2) + (k4 * 2) + (k5 * 2);

    cout << leisure << endl;

    return 0;
}